# Stub — Fase 0 (esqueleto). Base e etapas reais entram na Fase 2.
FROM python:3.11-slim

WORKDIR /app

# COPY pyproject.toml .
# RUN pip install --no-cache-dir .

COPY . .

CMD ["python", "-m", "src.ingest"]
