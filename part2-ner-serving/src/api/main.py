# Transporte REST (FastAPI) — fino, reusa nercore.service.
# Endpoints: /load/, /predict/, /list/, /models/, /health/, /models/{version}, /metrics.
# Ver ADR-0008. Ainda não implementado (esqueleto).
