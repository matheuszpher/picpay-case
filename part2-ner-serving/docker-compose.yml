# Stub — Fase 0 (esqueleto). Sobe api + redis (Prometheus/Grafana entram na Fase 3).
services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
